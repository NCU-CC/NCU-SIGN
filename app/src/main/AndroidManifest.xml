<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.edu.ncu.cc.ncunfc" >

    <uses-feature android:name="android.hardware.nfc" android:required="true" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".dummy.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dummy.NFCActivity"
            android:label="@string/title_activity_nfc" >
        </activity>
        <activity
            android:name=".dummy.NewCourseActivity"
            android:label="@string/title_activity_new_course" >
        </activity>
        <activity
            android:name=".dummy.CourseListActivity"
            android:label="@string/title_activity_course_list" >
        </activity>
        <activity
            android:name=".dummy.CourseDetailActivity"
            android:label="@string/title_activity_course_detail" >
        </activity>
        <activity
            android:name=".dummy.SignActivity"
            android:label="@string/title_activity_sign" >
        </activity>
    </application>

</manifest>
